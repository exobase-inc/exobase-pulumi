{
  "name": "@exobase/pulumi-aws-s3-static-website",
  "version": "0.0.1-alpha.10",
  "description": "Exobase pulumi package for creating resources in AWS that make up a static website on S3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage",
    "check": "yarn lint && yarn test && yarn build",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/mime": "^2.0.3",
    "@types/node": "^14",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "@pulumi/aws": "^4.0.0",
    "@pulumi/awsx": "^0.30.0",
    "@pulumi/pulumi": "^3.0.0",
    "cmdish": "^1.0.0",
    "fs-extra": "^10.0.0",
    "mime": "^3.0.0",
    "radash": "^1.4.0"
  }
}
